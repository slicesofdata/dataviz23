Convert png to ggplot object using "cowplot"

1. cowplot::ggdraw() 

2. cowplot::draw_image(here::here("project folder containing image", "my_image.png")) 


Use colorblindr::edit_colors() to change the colors of an object

1. colorblindr::edit_colors(my_plot, protan, sev = 0.8) # change the severity and type of colorblindness of plot

2. Or do and arrange in a grid also using cowplot

cowplot::plot_grid(my_plot, nrow = 1, # top left
                   colorblindr::edit_colors(p, protan, sev = 0.8), # top right
                   colorblindr::edit_colors(p, deutan, sev = 0.8), # bot left
                   colorblindr::edit_colors(p, tritan, sev = 0.8)  # bot right
                   )
