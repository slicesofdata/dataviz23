---
title: "Untitled"
author: "gcook"
date: "`r Sys.Date()`"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

# **Class Exercise: move to new file**

```{r}
#grep_NamesWith <- function(df, pattern = NULL) {
#  if (!is.null(pattern)) { names(df)[grep(pattern, names(df))]  }}
dat_file_path <- paste(data_dir,
                       "GCDS_Games_Survey_choice.csv", 
                       sep = "/")

GAME_DAT_choice <- readr::read_csv(dat_file_path)

# Make a bar plot

#GAME_DAT_choice %>%
#  ggplot(., mapping = aes(x = `most pleasurable`)) +
#  geom_bar()

# Make a point plot

# Make a column plot

```
