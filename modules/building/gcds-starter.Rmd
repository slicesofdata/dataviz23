---
title: ""
author: ""
date: "`r Sys.Date()`"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{R message=FALSE, warning=FALSE, include=FALSE}
# clean R objects
rm(list = ls(all.names = TRUE))

# set the paths for project, script, and data dirs
this_rmd_path = rstudioapi::getActiveDocumentContext()$path
proj_name = "??????" # ex stroop
proj_dir <- gsub("r/.*.Rmd","", this_rmd_path)
r_dir <- paste0(proj_dir, "r/")
data_dir <- paste0(proj_dir, "data/")
```


```{r message=FALSE, warning=FALSE, include=FALSE}
# load libraries
library(magrittr)
library(ggplot2)
```

```{r message=FALSE, warning=FALSE, include=FALSE}
# source the function file
source("https://pastebin.com/raw/b63hB7Hj") # read raw code 

# source the scripts for task data projects
source(paste0(r_dir, proj_name, "/read_data.R"))
source(paste0(r_dir, proj_name, "/clean_data.R"))
#source(paste0(r_dir, proj_name, "/model_data.R"))
```
